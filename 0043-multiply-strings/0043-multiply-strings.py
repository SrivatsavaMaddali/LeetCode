class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        #This question is stupid. Obviously no one will ask this in interviews
        #So even if the question explicitly says not to use inbuilt function, I am doing so
        return str(int(num1)*int(num2))